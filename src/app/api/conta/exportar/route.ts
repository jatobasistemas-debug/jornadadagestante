import {NextResponse} from 'next/server';import {isConfigured} from '@/lib/config';import {supabaseServer} from '@/lib/supabase/server';
export async function GET(){if(!isConfigured())return NextResponse.json({error:'Serviço indisponível'},{status:503});const db=await supabaseServer();const {data:{user},error}=await db.auth.getUser();if(error||!user)return NextResponse.json({error:'Acesso necessário'},{status:401});const tables=['profiles','pregnancies','private_memories','consent_records'] as const;const results=await Promise.all(tables.map(t=>db.from(t).select('*').eq('user_id',user.id)));if(results.some(r=>r.error))return NextResponse.json({error:'Não foi possível exportar seus dados.'},{status:500});const data=Object.fromEntries(tables.map((t,i)=>[t,results[i].data]));return new NextResponse(JSON.stringify({exported_at:new Date().toISOString(),email:user.email,...data},null,2),{headers:{'Content-Type':'application/json; charset=utf-8','Content-Disposition':'attachment; filename="minha-conta-jornada.json"','Cache-Control':'private, no-store'}});}
