import {publicClinic} from '@/lib/access';import {BrandShell} from '@/components/brand-shell';import {SignupForm} from '@/components/signup-form';
export default async function Signup({params}:{params:Promise<{clinic:string}>}){const {clinic:slug}=await params;const clinic=await publicClinic(slug);return <BrandShell clinic={clinic}><main id="conteudo" className="content narrow"><p className="eyebrow">Só o essencial para começar</p><h1>Vamos nos conhecer?</h1><section className="panel">{process.env.REGISTRATION_ENABLED==='true'?<SignupForm slug={slug}/>:<p>Os novos cadastros ainda não estão disponíveis. Em breve, sua clínica poderá orientar você.</p>}<p><a href={`/${slug}`}>Voltar para o acesso</a></p></section></main></BrandShell>;}
